{
  "name": "@ahm3tj4f/pi-undo",
  "version": "0.2.0",
  "description": "Undo/redo for pi, but this time it works. Per-message snapshots, ported from OpenCode's exact undo/redo philosophy.",
  "license": "MIT",
  "type": "module",
  "keywords": ["pi-package", "pi", "undo", "redo", "snapshots", "opencode"],
  "publishConfig": {
    "access": "public"
  },
  "files": ["index.ts", "src", "README.md", "LICENSE"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
